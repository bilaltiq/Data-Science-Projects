str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from(Weekday),
values_from(Mean_Time)
)
?pivot_wider
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
-Activity,
names_from = Weekday,
values_from = Mean_Time
)
kable(summary_table_mycal)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
-Activity,
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
arrange(Weekday)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(Weekday)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary, booktabs = TRUE)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
View(mycal)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
) %>%
kable(pivoted_summary, booktabs = TRUE)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
) %>%
kable(summary_table, booktabs = TRUE)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
) %>%
kable(summary_table, booktabs = TRUE)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
) %>%
kable(summary_table_mycal, booktabs = TRUE)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
) %>%
kable(summary_table_mycal, booktabs = TRUE)
View(summary_mycal)
View(summary_table_mycal)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(summary_table_mycal, booktabs = TRUE)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary, booktabs = TRUE)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(match(day.name,Weekday))
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(match(weekday.name,Weekday))
ggplot(homeActivities_plot, aes(x = Activity, y = Duration)) +
geom_boxplot(fill = "white", color = "grey") +
theme_minimal() +
coord_flip() +
labs(x = "Activity", y = "Duration (hours)",
title = "Distribution of Duration for Each Activity") +
facet_grid(~Location)
ggplot(homeActivities_plot, aes(x = Activity, y = Duration)) +
geom_boxplot(fill = "white", color = "black") +
theme_minimal() +
coord_flip() +
labs(x = "Activity", y = "Duration (hours)",
title = "Distribution of Duration for Each Activity") +
facet_grid(~Location)
ggplot(homeActivities_plot, aes(x = Activity, y = Duration)) +
geom_boxplot(fill = "pink", color = "black") +
theme_minimal() +
coord_flip() +
labs(x = "Activity", y = "Duration (hours)",
title = "Distribution of Duration for Each Activity") +
facet_grid(~Location)
ggplot(homeActivities_plot, aes(x = Activity, y = Duration)) +
geom_boxplot(fill = "blue", color = "black") +
theme_minimal() +
coord_flip() +
labs(x = "Activity", y = "Duration (hours)",
title = "Distribution of Duration for Each Activity") +
facet_grid(~Location)
ggplot(homeActivities_plot, aes(x = Activity, y = Duration)) +
geom_boxplot(fill = "white", color = "black") +
theme_minimal() +
coord_flip() +
labs(x = "Activity", y = "Duration (hours)",
title = "Distribution of Duration for Each Activity") +
facet_grid(~Location)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(match(month.name,Weekday))
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_label
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_label) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(match(name,Weekday))
?arrange
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary, booktabs = TRUE)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(Weekday)
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary, booktabs = TRUE)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
wrangled_table_mycal <- mycal %>%
select(
activity, duration_hours, weekday_number
) %>%
rename(
Activity = activity, Duration = duration_hours,
Weekday = weekday_number) %>%
mutate(Activity = case_when(str_detect(Activity, "physics") ~ "Physics",
str_detect(Activity, "math") ~ "Math",
str_detect(Activity, "cosc") ~ "Comp Science",
str_detect(Activity, "sleep") ~ "Sleep",
str_detect(Activity, "job") ~ "Job",
str_detect(Activity, "stat") ~ "Statistics",
TRUE ~ "Other")) %>%
filter(Activity %in% c("Job", "Physics", "Math", "Comp Science"
,"Statistics"))
wrangled_table_mycal$Duration = round(wrangled_table_mycal$Duration, 3)
summary_table_mycal <- wrangled_table_mycal %>%
group_by(Activity, Weekday) %>%
summarize(
Mean_Time = mean(Duration)
) %>%
arrange(Weekday) %>%
mutate(Weekday = case_when(str_detect(Weekday, "1") ~ "Monday",
str_detect(Weekday, "2") ~ "Tuesday",
str_detect(Weekday, "3") ~ "Wednesday",
str_detect(Weekday, "4") ~ "Thursday",
str_detect(Weekday, "5") ~ "Friday",
str_detect(Weekday, "6") ~ "Saturday",
str_detect(Weekday, "7") ~ "Sunday"))
pivoted_summary <- summary_table_mycal %>%
pivot_wider(
names_from = Weekday,
values_from = Mean_Time
)
kable(pivoted_summary, booktabs = TRUE)
# Code for table
# Only code for your table should be here (no or very minimal wrangling code)
